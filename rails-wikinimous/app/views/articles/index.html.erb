<div class="container">

  <% @articles.each do |article|%>
    <div class="article">
      <h2><%= link_to article.title, article_path(article) %></h2>
      <p><%=Kramdown::Document.new("#{article.content.slice(0,150)}&nbsp;[...]").to_html.html_safe%></p>
    </div>
  <% end %>
</div>

